class RecentItem {
  final String title;
  final String subtitle;
  final String tag; // Ví dụ: 'Thuốc', 'Bệnh'

  const RecentItem({
    required this.title,
    required this.subtitle,
    required this.tag,
  });
}